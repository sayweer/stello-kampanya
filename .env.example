# Stello's hosted relay. The campaign nudges it so a pledge lands without
# waiting for the relay's next pass. This app never holds the landing key.
NEXT_PUBLIC_STELLO_RELAY_URL=http://localhost:3000/api/relay
